abstract class ModelStreamService {
  void execute();
}

class ModelStreamServiceImpl implements ModelStreamService {
  /// INJECTABLE NETWORK & HIVE
  @override
  void execute() {
    /// CHECK INTERNET STATE
    /// CHECK SERIVCE NETWORK STATE
    /// CHECK HIVE SERVICE STATE
  }
}
